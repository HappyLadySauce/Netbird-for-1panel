# Golden reference (NetBird latest)

Generated from official `getting-started.sh` with:

- `REVERSE_PROXY_TYPE` equivalent: exposed ports (`render_docker_compose_exposed_ports`)
- `BIND_LOCALHOST_ONLY=true`
- `NETBIRD_DOMAIN=test-netbird.example.com`

Use when diffing `init.sh` / compose changes against upstream.
